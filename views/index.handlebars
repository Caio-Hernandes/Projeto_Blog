<div class="card p-3">
  <h1 class="display-4">Bem vindo ao Blog do Node</h1>
  <p class="lead">
    Esse projeto foi desenvolvido em Java Script, NodeJS, Express e MongoDB.
  </p>
  <hr class="my-3">
  <p class="mb-2">
  </p>
  <div class="mt-4">
    <a class="btn btn-success" href="/usuarios/registro" role="button">Crie uma conta</a>
  </div>
</div>

<hr>

<h2 class="titulo mt-4">Postagens recentes:</h2>

{{#each postagens}}
<div class="card mt-3">
    <div class="card-body">
      <h3 class="titulo">{{titulo}}</h3>
      <p>{{descricao}}</p>
      <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
      <hr>
      <small class="texto-especial">Categoria: {{categoria.nome}}</small><br>
      <small>Data de publicação:{{formatDate data}}</small>
    </div>
  </div>

{{else}}
<br>
<h5>Nenhuma postagem</h5>

{{/each}}